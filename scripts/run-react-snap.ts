// scripts/run-react-snap.ts
// This project is now a standard Vite SPA. This file exists only because
// legacy scripts reference it (build:ssg). We intentionally no-op.

// eslint-disable-next-line no-console
console.log("[run-react-snap] Skipped (SPA build)");
